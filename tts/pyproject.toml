[project]
name = "tts"
version = "0.1.0"
description = "Generate word audio from data/words.json with Google Cloud TTS"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "google-cloud-texttospeech>=2.36.0",
]
